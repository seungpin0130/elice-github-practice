---
description: 현재 브랜치를 푸시하고 PR을 생성한다
argument-hint: [PR 제목]
---

1. 현재 브랜치 이름을 확인한다. `main`이면 중단하고 알려준다.
2. 현재 브랜치를 원격에 푸시한다.
3. `gh pr create`로 PR을 만든다. 제목: $ARGUMENTS
4. 본문에 아래 두 섹션을 포함한다.
   - `## 변경 내용`
   - `## 확인 방법`
5. 생성된 PR 링크를 출력한다.
